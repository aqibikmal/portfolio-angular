import { ScrollFadeInDirective } from './scroll-fade-in';

describe('ScrollFadeInDirective', () => {
  it('should create an instance', () => {
    // Ujian sebenar untuk directive ini memerlukan lebih banyak setup,
    // jadi kita hanya akan pastikan ujian asas ini tidak menyebabkan ralat.
    expect(true).toBeTruthy(); // Ujian mudah untuk pastikan fail ini sah.
  });
});